---

import { ArrowRight, Lightbulb, Sparkles, Star } from "@lucide/astro"
import { fetchFromApi } from '../lib/api';

const posts = await fetchFromApi('api/home');
const post = posts.data[0];
---

<!-- <img id="background" src={background.src} alt="" fetchpriority="high" /> -->

<main>
  <div class="flex items-center justify-center min-h-screen">
    <section class="container mx-auto px-4 py-16 md:py-24">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div class="space-y-6">
        <div class="inline-flex items-center px-3 py-1 rounded-full bg-purple-100 text-purple-700 text-sm font-medium">
          <Sparkles class="h-4 w-4 mr-2" />
          <span>Consejos diarios para mejorar tu vida</span>
        </div>
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 leading-tight">
          Descubre el <span class="text-purple-600">Consejo del DÍA</span>
        </h1>
        <p class="text-lg text-gray-600">
          Consejos prácticos y efectivos para ayudarte a mejorar cada aspecto de tu vida diaria. Desde productividad
          hasta bienestar personal.
        </p>
        <div class="flex flex-row gap-4">
          <a href="/posts" class="flex flex-row items-center bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg font-medium">
            Más consejos del día
            <ArrowRight class="ml-2 h-4 w-4" />
          </a>
          <button class="border-purple-200 text-purple-600 hover:bg-purple-50">
            Explorar categorías
          </button>
        </div>
      </div>
      <div class="relative">
        <div class="absolute -top-6 -left-6 w-24 h-24 bg-yellow-100 rounded-lg z-0"></div>
        <div class="absolute -bottom-6 -right-2 sm:-right-6 w-24 h-24 bg-purple-100 rounded-lg z-0"></div>
        <div class="relative z-10 bg-white p-6 rounded-xl shadow-lg">
          <div class="flex items-center space-x-2 mb-4">
            <Lightbulb class="h-6 w-6 text-yellow-500" />
            <h2 class="text-xl font-bold text-gray-900">{post.post_name}</h2>
          </div>
          <p class="text-gray-700 mb-6">
            {post.excerpt}
          </p>
          <div class="flex items-center justify-between">
            <div class="flex items-center space-x-1">
              <Star class="h-4 w-4 fill-yellow-400 text-yellow-400" />
              <Star class="h-4 w-4 fill-yellow-400 text-yellow-400" />
              <Star class="h-4 w-4 fill-yellow-400 text-yellow-400" />
              <Star class="h-4 w-4 fill-yellow-400 text-yellow-400" />
              <Star class="h-4 w-4 fill-yellow-400 text-yellow-400" />
            </div>
            <span class="text-sm text-gray-500">{post.published_at}</span>
          </div>
        </div>
      </div>
    </div>
  </section>    
</div>
</div>
</main>
